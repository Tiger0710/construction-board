# 工事予定表 全体追加点検（2026-09-10）

## 点検と修正

- 全月共通データ、競合保存、部分読込失敗、不正応答、429、timeout、入力・日別設定、会社別ページ切替をレビュー。
- 廃止済みExcelのupload/files/create-month API、upload.html、月別Excel生成workflowを削除。利用者の「Excel利用者はいない、案内も不要」という指示に基づく。旧APIに残っていたパス入力・型チェック・workflow引数の問題は公開経路ごと撤去。
- 同じ工事の直前の連続期間を追加した場合にも重複警告を表示。取消で既存案件を編集する。
- 日付が変わった直後の「本日」誤表示を修正。取得済み予定を時計から再構成し、5分周期を待たず更新。
- 1024幅のヘッダー重なりを修正。
- 通信失敗・30秒timeout時は前回取得した予定を維持し、古いデータであることを表示。復旧時は警告解除。

## 実行した検証

- `node --test tests/data-api.test.mjs tests/project-merge.test.cjs`: 52 PASS。
- `node tests/month-boundary.cjs`: 12 PASS。
- `node tests/default-shift.cjs`: 25 PASS。
- `node tests/month-merge-ui.cjs`: 17 PASS。逆方向の連続期間は修正前FAILを再現。
- `node tests/company-rotation.cjs`: 22 PASS。日跨ぎ、1024幅、通信失敗、再読込cache、timeout、復旧を含む。
- 合計128 PASS、0 FAIL。`git diff --check` PASS。
- 既存データは変更していない。本番全7担当814件のAPI応答と整理済みデータを比較し一致。本日12件・明日16件の内容不変、ブラウザ例外なし。
- 本番data関数ログを追加点検中に観測し、取得できたログに例外/OOM/timeoutなし。これは観測時間内の結果であり、過去の全ログを対象とするものではない。

## 確認範囲の限界

本番の実データを使ったテスト書込は行っていない。保存・競合・失敗時の動作はmock回帰テストおよび前回の隔離testブランチ実保存テストで検証している。

現行画面/APIは従来からログイン認証・担当者別権限制御がない。URLを知る人の閲覧・編集を制限する要件には対応していない。今回、ログインを新設して利用方法を変更することはしていない。

Fire TV実機は操作していない。Chromeで1920・1366・1024幅を確認した。

## 本番反映後

コード `c1fcc31`、Actions `34432668949` 成功。本番uploadページと旧3APIはすべて404。1024幅の本番ヘッダー非重複、通信警告非表示（正常接続）、ブラウザ例外なしを確認。全7担当814件、本日・明日28件の内容一致、HSJ→コーワ→光洋の本日/明日6グループ切替を再確認した。
